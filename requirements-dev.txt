# Install alongside requirements.txt to run the smoke tests:
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest tests/ -v
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
asgi-lifespan==2.1.0
mongomock-motor==0.0.35
